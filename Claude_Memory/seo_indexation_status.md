---
name: seo-indexation-status
description: واقع فهرسة dealpulseksa.com من GSC — قفزت 4→150 مفهرسة (2026-06-30)؛ العائق الباقي 244 «مكتشفة لم تُفهرس»؛ أولويات الإصلاح
metadata: 
  node_type: memory
  type: project
  originSessionId: 00627dc4-16c9-4bf4-aeeb-45afe0de38c7
---

تشخيص GSC لـ www.dealpulseksa.com بتاريخ 2026-06-21:

**الواقع:** الـsitemap فيه **139 رابط** (11 ثابتة + 40 متجر + 28 تصنيف + 16 مدوّنة + 44 صفحة /c/)، لكن قوقل **فهرس 4 فقط**. الأداء 3 شهور: 141 ظهور / 4 نقرات، **كله اسم العلامة** (/about + الرئيسية). طلبات البحث **مجهّلة (لا بيانات)** → لا يمكن تأسيس المحتوى على GSC؛ نستخدم [[google_ads_keyword_planner]].

**السبب الجذري:** حصار الموقع الجديد (عمره أسابيع) + **باكلينك بلا قيمة** + ثقة دومين صفر → ميزانية زحف شبه معدومة. ⚠️ تصحيح (Ahrefs 2026-06-21): «صفر باكلينك» كان خطأ — فعلياً **41 ref domains / 43 backlinks لكن DR=0 + 0 كلمات + 0 ترافيك** = روابط شبه عديمة القيمة (سكرابرز/سبام). المشكلة = لا باكلينك **ذو قيمة**. (Ahrefs المجاني يفحص موقعنا فقط؛ بيانات المنافسين مقفولة خلف المدفوع.) **ليست** مشكلة كود/Schema/محتوى. أسباب عدم الفهرسة: 1×(404) + 4×(canonical بديل=طبيعي) + 0×(زُحف ولم يُفهرس).

**الإيجابيات:** HTTPS 8/0 · Breadcrumbs صالحة 6/0 · الخريطة «نجح» · سعودي 137 + جوال 96 · متوسط الموضع 1.8 (علامة).

**الأولويات:** P0 طلب فهرسة يدوي للهَبّات (/stores /deals /trending الرئيسية) + أفضل /c/ + أبرز المتاجر · P0 روابط خلفية (5-15 حقيقي) · P1 إصلاح صفحة الـ404 · Schema/llms.txt ثانوية.

**أُنجز:** حذف AggregateRating المصطنع من lib/seo/schema.ts (مخالفة سياسة Google) · استبدال llms.txt المفبرك (كان يدّعي «آلاف الكوبونات» ومتاجر غير موجودة نون/أمازون/شي إن + رابط /categories مكسور) بـ Route Handler ديناميكي صادق app/llms.txt/route.ts. يكمّل [[domain_authority_plan]] ضمن قيد [[seo_white_hat_only]].

**Ahrefs Site Audit (2026-06-21):** 162 URL مزحوف كلها 2xx، صفر 4xx/5xx → الموقع سليم تقنياً، **لا شي من الـ17 issue هو سبب 4/139** (السبب يبقى الباكلينك). فرز: أغلبها ضجيج تجميلي (meta desc/title/OG/word-count). «128 schema validation error» = **false-positive من Ahrefs الصارم** (مراجع @id السليمة أو نوع OnlineBusiness)، مو بسببنا (حذفنا AggregateRating/Product) → المرجع Rich Results Test من قوقل لا Ahrefs. **الوحيد الحقيقي = Orphan pages ×7 = صفحات /c/ الإنجليزية (-en)** (تُربط من الإنجليزية فقط + related guides تغطّي 6 → البقية يتيمة). **القرار: noindex+follow للإنجليزية + حذفها من sitemap** (web commit 1307350) — طلبها ضعيف (جمهور عربي) ووجودها يبدّد ميزانية الزحف. + فعّل IndexNow من Ahrefs (Bing/Yandex مجاناً).

**تصحيح + تنفيذ (2026-06-24):** ادّعاء 2026-06-21 أن «schema validation error» = false-positive من Ahrefs كان **خطأ**. السبب حقيقي ومؤكّد من schema.org: عقدة `localBusinessNode()` نوعها `OnlineBusiness` (ابن Organization، **لا** LocalBusiness) لكنها كانت تحمل `priceRange` + `currenciesAccepted` — وهما **خاصّتان حصريّتان بـ LocalBusiness** → خاصّية غير صالحة على النوع = الخطأ على كل الـ130 صفحة (الـ@graph العام يُحقن بكل صفحة من app/layout.tsx). **الإصلاح (web commit 6a9d5ed):** حذفهما؛ إشارة المجانية محفوظة في `softwareApplicationNode().offers.price=0`. ⚠️ لا تُعِد priceRange/currenciesAccepted لأي نوع ليس LocalBusiness. **IndexNow (صحّحت نفسي بنفس الجلسة):** IndexNow **لم يكن ميّتاً**. ملف المفتاح `797a6c846fb073868c5a80577f00dc0ce9feccc1bd6fb8accee47e7a6e4d35e4.txt` مستضاف بالريبو منذ Jun 1، مطابق لـ INDEXNOW_KEY على Railway، ومتحقّق حيّاً (الرابط يرجّع المفتاح). أداة Glob أعطتني false-negative فظننت الملف غائباً وولّدت مفتاحاً ثانياً (e294…) — أزلته. **الدرس: تحقّق من القرص/git مباشرة قبل استنتاج «غائب» من Glob.** الملكية صالحة، فلا خطوة يدوية مطلوبة على Railway. السؤال الحقيقي المتبقّي: هل `submit_page`/`resubmit_url` تُستدعى فعلاً عند النشر؟ وعلى أي حال IndexNow يخصّ Bing/Yandex/Naver/Seznam **لا Google** — فلا يحرّك 4/139 (مشكلة قوقل تبقى باكلينك + طلب يدوي). أضفت INDEXNOW_KEY (القيمة الصحيحة) إلى .env المحلي للتماثل + تعليق في .env.example (main a3e7bab). **بنود Ahrefs التجميلية (web commit 6aa77f1):** أنشأت `lib/seo/metadata.ts` → `buildMetadata()` مصدر مركزي للميتاداتا (عنوان ≤60 absolute + وصف 110-158 + OG كامل مع og:url=canonical + twitter)، وحوّلت 15 صفحة له. أطفأ: Meta desc too long/short، Title too long، OG incomplete، OG≠canonical + canonical مفقود لـ/categories. **قاعدة: أي صفحة جديدة تستخدم buildMetadata لا تكتب كائن metadata يدوياً.** الباقي بـAhrefs غير قابل/لا يُصلَح: redirects (طبيعية www/https)، word count changed (إخباري)، titles don't match (قوقل يعيد الكتابة)، x-default hreflang (مؤجّل حتى /en)، و«صفحة برابط داخلي واحد ×24» = دَيْن ربط داخلي بنيوي (لا ميتاداتا).

**⚠️ تنبيه دقّة (2026-06-24، أكّده المالك):** نقرات/ظهورات GSC حتى الآن (20 نقرة/190 ظهور) **كلها ترافيك المالك نفسه** — لا زوّار خارجيون جدد (تحليلات الموقع تؤكّد لا أحد جديد). فلا تُقرأ نقرات GSC كتحقّق سوق/جذب؛ المنصّة ما زالت قبل-الجذب. (أنا بالغت في تفاؤل القراءة الأولى — تصحيح.) **breadcrumb GSC (الحقل item غير مضمَّن ×6 على /c):** السبب مسارَا تنقّل لكل صفحة /c (واجهة + باكند)، ونسخة الباكند رابطها العربي خام غير مُرمَّز (`_abs_url` بلا quote) فرفض قوقل item. **الإصلاح:** الواجهة صارت المصدر الوحيد — `stripBreadcrumb` يزيل breadcrumb الباكند من page.jsonld وقت العرض (web 389d6b6)، والباكند أوقف توليده + رمّز _abs_url + حذف الدوال الميتة (main 6ac48a2). + /categories أُضيف للـ sitemap (270407c).

**تحديث (2026-06-29):** الـsitemap كبر لـ**402 رابط** (كان 139). لقطات GSC المتدحرجة 28يوم في `seo_perf_snapshots` تُظهر صعوداً ظاهرياً: نقرات 2→26 وظهور 89→223 خلال أسبوعين، CTR 12%، متوسط الموضع «نزل» 1.8→12 (= توسّع بصمة الكلمات لا تراجع). **لكن المالك أكّد ثانيةً: الدفعة كلها «نشرته على أصحابي دخل كم واحد وبعدين ماحد دخل»** — أي بحث براند من شبكته الشخصية، **صفر اكتشاف عضوي من غرباء**، وثبتت بعد الدفعة. (بالغتُ بالتفاؤل في القراءة الأولى — صحّحت.) السؤال الحاسم المتبقّي: كم نقرة **Non-Branded**؟ يحتاج تفصيل per-query (لم يُسحب بعد — `GSC_SA_JSON` على Railway لا محلياً). **قسم «🔎 الفهرسة» الجديد (main 50219b1):** صفحة داشبورد تسحب 402 رابط من sitemap الحيّ، المعلّق = sitemap ناقص المعالَج، workflow يدوي (انسخ→GSC URL Inspection→Request Indexing→«تمّت» فيختفي) + تجاهل/تراجع/فلترة/ترقيم. migration 063 جدول `seo_index_queue` (يخزّن المعالَج فقط، status: indexed|ignored). يخدم أولوية P0 «طلب الفهرسة اليدوي».

**الـ404 الغامض انحلّ (2026-06-29):** الـ404 الوحيد اللي كان GSC يبلّغ عنه = متجر «ذا ديل» (master.id=11) اسمه مخزّن بمسافة زائدة `"ذا ديل "` (طول 7 بدل 6) → الموقع يولّد `/store/ذا ديل%20` فيرجّع 404. القيمة المعطوبة كانت في 3 جداول: master(1)+action_logs(4)+social_posts_log(8). **الإصلاح:** `UPDATE … SET store_id=btrim(store_id)` على الثلاثة (بإذن المالك). تحقّق: الرابط النظيف `/store/ذا ديل` يرجّع 200، والـ API (api.dealpulseksa.com يقرأ DB مباشرة بلا كاش) صار يبثّ النظيف، صفر store_id بمسافات في الـ45 متجر. **الكاش الوحيد المتبقّي = Next/Vercel ISR على sitemap.xml** (يتجدّد تلقائياً ≤يوم؛ changefreq=daily). **درس بياناتي: افحص مسافات btrim على أي حقل يدخل URL.** يكمّل قسم [[seo-indexation-status]] «🔎 الفهرسة».

**📈 قفزة الفهرسة (2026-06-30، لقطة GSC أكّدها المالك):** المفهرسة **4→150** خلال 9 أيام — أكبر تحوّل حتى الآن (ثمرة طلبات الفهرسة اليدوية عبر `seo_index_queue` + توسّع sitemap لـ402 + الإصلاحات التقنية schema/canonical/breadcrumb). غير المفهرسة **255** موزّعة: **244 «تم اكتشاف الصفحة – لم تُفهرس بعد» (Discovered, currently not indexed)** = العائق الأول (قوقل يعرف الروابط من الـsitemap لكن ميزانية الزحف/ثقة الدومين لا تكفي لزحفها) · 7 «زُحف ولم يُفهرَس» (Crawled, not indexed = قوقل زحفها ورآها غير جديرة → صفحات رقيقة/سلطة ضعيفة) · 1×404 · 1 canonical اختاره قوقل · 2 صفحة بديلة بـcanonical سليم (طبيعي). **القراءة:** الفهرسة لم تعد العنق — 150 صفحة قابلة للظهور الآن. العنق انتقل إلى (أ) **سلطة الدومين/الباكلينك** لفكّ الـ244 (طلب الفهرسة اليدوي لا يتوسّع على 244، الحل بنيوي = روابط خلفية ذات قيمة + ربط داخلي أعمق لصفحات /c والمتاجر — انظر [[domain_authority_plan]] و backlink_targets.md) و(ب) **الجودة** لتقليل الـ7 المزحوفة المرفوضة. ⚠️ **تبقى قاعدة المالك:** مفهرَس ≠ ترافيك؛ الظهور/النقر ما زال أغلبه براند من شبكته الشخصية، والسؤال الحاسم يبقى نقرات Non-Branded من غرباء (لم يُسحب per-query بعد — `GSC_SA_JSON` على Railway).

**🔗 أصل الباكلينك الأول شُحن (2026-07-03):** نُشرت صفحة **`/calendar` «تقويم تخفيضات السعودية»** (web commit 303c744) — أعلى رافعة White-Hat في `backlink_targets.md` طبقة D: مورد مرجعي قابل للاستشهاد (12 موسم + FAQ + ItemList/FAQPage schema + ربط داخلي لـ/deals و/stores و/trending والدليل المحوري، مضاف لـ STATIC_PAGES→sitemap). server-rendered عربي، صفر فبركة (تواريخ هجرية موسومة «تقديري»). **المتبقّي = الترويج لاكتساب روابط إليه** (طلب فهرسة GSC + ذِكر في الأدلة/المجتمعات/guest posts) + بقية طبقات A/B/C من `directory_submission_kit.md`. يخدم فكّ الـ244 «مكتشفة لم تُفهرس». يكمّل [[domain_authority_plan]].

**🌐 بروبرتي GSC ثانية «sc-domain:dealpulseksa.com» (2026-07-03):** غير بروبرتي www-prefix؛ نطاق كامل يشمل www+non-www+http+**api**. 154 مفهرسة / 270 غير. جديدها: **«خطأ في إعادة التوجيه» ×10** (أدلة صحية+/blog+/stores+/trending+/about). **السبب (مفحوص حيّاً):** الموقع على **Vercel**، والتحويل non-www→www **edge-level 307 مؤقّت** (مو في الكود — لا middleware/next.config)، + **cold-start** يعلّق أحياناً (شوهد hang دقيقتين ثم عاد 0.8s) → قوقل يسجّل «خطأ توجيه». **✅ أُصلح (2026-07-03): على Vercel → Domains → dealpulseksa.com → Edit → Redirect status = «308 Permanent Redirect» بدل 307.** مؤكّد حيّاً (308 لـwww، 0.7-1.2s). المتبقّي: GSC «التحقق من الإصلاح» على «خطأ التوجيه» بعد إعادة زحف قوقل. (كان منخفض الخطورة أصلاً — الصفحات مفهرسة عبر www.) بقية البنود: «صفحة بتوجيه ×3»=توحيد host طبيعي · 404×2 = /en (محذوف بقصد)+/store/2 (قديم) · «زُحف لم يُفهرَس» فيها **api.dealpulseksa.com** (يحتاج noindus/robots للـAPI) · 244 مكتشفة=عنقود [[blog_aliexpress_cluster]] (يُعالَج).

**حزمة الفهرسة (2026-06-21):** (1) فحص الكود: صفحات store/[slug] و c/[slug] تستدعي `notFound()` صح + not-found.tsx سليمة → **لا soft-404**؛ الـ404 الواحد بGSC = رابط محدّد يحدّده المالك من تقرير Pages. (2) إصلاح sitemap.ts (web commit 63cf5d0): الصفحات الثابتة كانت تبثّ `lastModified=now` كل ساعة (تاريخ كاذب) → الآن lastmod للصفحات الحيّة فقط (hourly/daily). (3) وثيقتان في الريبو الرئيسي **`seo/`**: `gsc_indexing_playbook.md` (طلب فهرسة يدوي بطبقات يومية، حصة ~10/يوم) + `backlink_targets.md` (4 طبقات White-Hat: أساسات علامة + أدلة سعودية موثّقة saudibizness/listinsaudi/b2bmap/saudiayp + ملفات أفلييت سلة/Admitad + محتوى مكتسَب «تقويم تخفيضات السعودية»). **المتبقّي = تنفيذ المالك اليدوي** (طلبات GSC + اكتساب الباكلينك على 4-6 أسابيع).
