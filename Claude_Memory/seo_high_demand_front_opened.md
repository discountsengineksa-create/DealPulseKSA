---
name: seo_high_demand_front_opened
description: كشف فخّ «AR draft / EN noindex» يُخفي براندات الطلب-العالي؛ فُتحت 4 (علي اكسبرس/علي بابا/ستايلي/إيرالو) 2026-07-12
metadata:
  type: project
---

**السياق:** المالك أحبط من لقطة Semrush (AI=0، لا تقدّم). سحبنا Search Console الحقيقي:
الظهور صعد ~250× (‏2/يوم أواخر مايو → 507/يوم 10 يوليو) لكن كله على **براندات صغيرة
محلية** (فوغا/سيدار/ماماز pos 7-25). ولا ظهور واحد على كلمات الطلب-العالي.
ملاحظة: AI=0 في Semrush **وهم قياس** (نطاق الودجت United States/إنجليزي؛ موقعنا عربي سعودي).

**الاكتشاف الجذري (فخّ منظومي في محرك /c/):**
مولّد صفحات الهبوط ([[project_overview]] api/seo/) يُنشئ العربي **draft** (نشر يدوي بعد
مراجعة) والإنجليزي **published** — لكن `/c/[slug]` يجعل EN **noindex** (`page.lang !== 'en'`
في app/c/[slug]/page.tsx). النتيجة: أي براند صفحته العربية بقيت draft = **صفر صفحة
عربية قابلة للفهرسة** (المنشور الوحيد noindex). وقعنا فيه 4 براندات طلب-عالي نملكها
بأفلييت+كوبون: علي اكسبرس، علي بابا، ستايلي، إيرالو.

**نُفِّذ 2026-07-12 (بإذن كتابة صريح، على seo_landing_pages فقط):**
1. UPDATE عناوين للهجاء العربي (السعودي يكتب «علي اكسبرس» لا «aliexpress»): ids 95/51/73.
2. إصلاح خلل ستايلي (كلمة فرنسية مسرّبة «répond» → «سنجيب»)، id 73.
3. نشر عبر endpoint الإنتاج `POST /api/v1/admin/seo-publish/{id}` (ids 95,51,73,101) —
   يعمل revalidate(200) + IndexNow + Google Indexing(200/ok). تحقّق end-to-end:
   الأربع live HTTP 200، robots=index,follow، العناوين الجديدة ظاهرة.

**السقف الصادق:** نون/نمشي صفحاتهم العربية **منشورة أصلاً** لكن يستهدفون أصعب كلمتين
(«كود خصم نون/نمشي») — محجوبون بالسلطة (Authority Score=0)، لعبة باكلينك/شهور لا on-page.
الأربعة المفتوحة = الشريحة **القابلة للكسب** من الطلب-العالي (براند + long-tail).

**الموجة ٢ (نفس اليوم، بإذن دفعة صريح):** الفخّ كان **على مستوى الموقع** — ١٩ من ٤٦
متجراً بلا أي صفحة عربية منشورة (AR draft فقط + EN noindex). نُشرت كلها (١٩/١٩ →
revalidate 200 + Google 200) بعد إصلاح العناوين للهجاء العربي «كود خصم {اسم عربي} 2026».
اصطدنا bug بيانات: m14 اسمه العربي «اتش اند ام» (H&M) لكن name_en مكتوب فيه كود الكوبون
«ADMIT10» فاستهدفت الصفحة «كود خصم ADMIT10» — أُصلح. **الصفحات العربية المنشورة: 27 → 50**
(+23). ملاحظة: خيارات (m20) له مسودّتان AR (57 نُشرت، 63 تُركت). العطل: حاجب الأمان
(claude-opus-4-8) تعطّل مؤقتاً بين الموجتين فتأخّرت الدفعة، لا علاقة بالكود.

**المتبقّي (روافع لم تُنفَّذ):** (أ) تعميق الصفحات (كلها ~200-320 كلمة، تحت هدف المحرّك
600-1000)؛ (ب) ربط داخلي مباشر من عناقيد المدوّنة الراكنة للصفحات التجارية؛ (ج) باكلينك/سلطة
(العنق الحقيقي للكلمات الكبرى نون/نمشي). **قرار مبدئي:** لا توليد صفحات long-tail متعددة
لكل متجر = doorway/thin يخالف White-Hat. راجع [[keyword_demand_ksa]] و[[seo_indexation_status]]
و[[content_guardrails_playbook]].

**فخّ ثانٍ من العائلة نفسها — الكلمة العربية بُنيت من `name_en` (٢٠٢٦-٠٨-٢٥):**
`select_top_demand_stores` في `api/seo/auto_pipeline.py` كانت تأخذ
`COALESCE(NULLIF(m.name_en,''), m.store_id)` ثم تحقنه في قالب عربي
`كود خصم {store} 2026`. النتيجة **٣٦ صفحة عربية منشورة بكلمة لاتينية**
(«كود خصم VPerfumes 2026»، «كود خصم Zid 2026») لا يكتبها سعودي — ونزيه كان
سيصير الـ٣٧. أُصلح الموضعان اللذان يبنيان أنماطاً عربية (`auto_pipeline.py`
و`seo_seed_custom` في `api/routers/admin.py`) بتفضيل `store_id` العربي —
الالتزام `85da13d`. تحقّق ما قبل الدفع (§٤.٩): تشغيل الدالة حرفياً على
الإنتاج أعطى `store_name='نزيه'` ⇒ «كود خصم نزيه 2026».
**العبرة المتكرّرة: مولّد الصفحات يُخطئ صامتاً في البيانات لا في الكود** —
نفس عائلة بق «ADMIT10 اسماً لـH&M» أعلاه. افحص عيّنة من `target_keyword`
بعد أي دورة توليد. الاستعلام:
`select target_keyword from seo_landing_pages where lang='ar' and target_keyword ~ '[A-Za-z]';`
