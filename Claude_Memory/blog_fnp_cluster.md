---
name: blog_fnp_cluster
description: عنقود فيرنز اند بيتل (FNP) — 16 مقالاً بادئة fnp- + تصحيح store_id (migration_074)؛ master.id=82 كود FN68 خصم 12% بوستيني؛ صفحات /c/ ومايقريشناتها أُلغيت 2026-09-09
metadata: 
  node_type: memory
  type: project
  originSessionId: 076662a2-adf1-4eae-bc64-d60612128b80
  modified: 2026-09-09
---

**2026-09-08** — المالك أرسل لقطات fnp.sa وطلب «سو السيو حقه». FNP كان أُضيف
للكتالوج قبله بيوم (`first_time=2026-09-07`).

## المتجر — `master.id=82`

- `store_id` صُحّح **`فيرنز اند بيتلز` → `فيرنز اند بيتل`** (بلا ز) — `migration_074_fnp_store_id_fix.sql`.
  Autocomplete سعودي: `فيرنز اند بيتلز` يصحّحه جوجل تلقائياً، و`فرنز اند بتلز`/`فيرنز ان بيتلز`
  = صفر، بينما `كود خصم فيرنز اند بيتل` اقتراح حيّ. صُحّح في `master` + النسخة المكرّرة
  في `social_posts_log.store_id` (٨ صفوف؛ الديدَب على `master_id` لا النصّ فالتغيير تجميلي).
  تُركت: `logo_url`/`social_poster_url` (مفاتيح Cloudinary تعمل مستقلّةً)، `post_text`،
  `api_request_metrics.path`. → [[seo_verify_brand_transliteration]]
- كود `FN68` خصم **12% على كل المنتجات** · `my_coupon='8%'` عمولة · بوستيني ·
  `affiliate_link=https://www.fnp.sa` (المالك ثبّته صراحةً — الأكواد قبل روابط التتبّع).
- `store_tags={ورد,هدايا,عطور}` · `seo_enabled=True` · توصيل يصل ٦٠ دقيقة الرياض/جدة + نفس اليوم.
- **الطلب الحقيقي بالإنجليزي/الاختصار:** `fnp` و`اف ان بي` autocomplete غنيّ (`fnp كيك`،
  `fnp ورد`، `fnp كود خصم`، `اف ان بي الرياض`). + رافعة كبرى عامة: `توصيل ورد الرياض نفس اليوم`.
  أقرب لمودانيسا (طلب حقيقي) لا لماكس/بوما (ملاحي). → [[blog_maxfashion_puma_clusters]]

## عنقود المدوّنة — 16 مقالاً `fnp-` (web repo، دُفع 2026-09-08)

هَب `fnp-flowers-gifts-guide-saudi` + توصيل سريع الرياض/جدة · دليل أنواع الورد
(جوري/توليب/عباد الشمس/أوركيد) · ورد+كيك · 6 مناسبات (تخرّج/مولود/شفاء/ذكرى
زواج/ميلاد/مباركة+منزل جديد) · هدايا له · هدايا فخمة (عطر+مجوهرات) · بوكسات
شوكولاتة · كود FN68 · مقارنة مع محلات الورد المحلية.

- **الزاوية:** ورد + توصيل فوري — لتفادي تكاذُب عنقود ميرايا (هدايا عامة، 9 مقالات)
  وعناقيد العطور. يربط لميرايا/سقابي/دليل العطور **لا ينافسها**. → [[seo_c_store_cannibalization]]
- صفر فبركة: لا أسعار مخترعة، حقائق FNP من store_bio + لقطات المالك (الموقع محجوب للبوتات).
  9–13 رابطاً داخلياً/مقال، إفصاح أفلييت أعلى كل مقال.
- تحقّق: esbuild يبني `lib/blog.ts` نظيفاً · صفر `${`/```` ``` ```` · 65 رابط `/store/` كلها `فيرنز اند بيتل`.

## blog_bridge — أُعيد بناؤه (بإذن المالك، DB write)

`python -m scripts.build_blog_bridge --write` → **1515 صفّ / 67 متجراً** (كان 1484/65).
كل `fnp-*` → `{فيرنز اند بيتل}` نظيفاً (16 صفّاً). → [[search_intelligence_layer]]
⚠️ مصنّف الوضع التلقائي بالهارنس يحجب `--write` — شغّله المالك بنفسه.

## صفحات /c/ — ⛔ أُلغيت بالكامل (٢٠٢٦-٠٩-٠٩)

`migration_075/076` أنشآ ٤ صفحات هبوط لـ id=82 (`كوبون فيرنز اند بيتل` · `توصيل
ورد الرياض` · `توصيل ورد جدة` · `فيرنز اند بيتل`، id 211-214). **هذا كان النمط
المضادّ الذي رفضه المالك في الجلسة نفسها** («في شي خطا... ما أبي التوليد عن طريق
توليد الصفحات») ثم رصده ثانيةً ٢٠٢٦-٠٩-٠٩ («مستحيل أسوّي مايقريشن على كل مقال»).

**التصحيح ٢٠٢٦-٠٩-٠٩:** `git rm migration_075 migration_076 scripts/queue_fnp_seo_pages.py`
+ حذف كامل لأنبوب التوليد التلقائي ([[seo_page_portfolio_verdict]] §٢٠٢٦-٠٩-٠٩).
الصفوف الأربع تُحذف من الإنتاج بأمر psql (الهارنس يحجب كتابة DB):
`DELETE FROM seo_index_submissions WHERE landing_page_id IN (SELECT id FROM seo_landing_pages WHERE master_id=82); DELETE FROM seo_landing_pages WHERE master_id=82;`

**الباقي من جلسة FNP سليم ومحفوظ:** `migration_074` (تصحيح store_id) + عنقود
المدوّنة ١٦ مقالاً + de-orphan (web `44d73c8`).

## تدقيق ٢٠٢٦-٠٩-٠٨ (المالك طبّق migration_075 + `blog_bridge --write` وطلب التحقّق)

**سليم:** ١٦ مقالاً في مصفوفة `posts` · إفصاح أعلى كلٍّ · صفر ```` ``` ````/`${` · كل روابط
`/blog/` تُحلّ · **٦٥ رابط `/store/` كلها `فيرنز اند بيتل`، صفر `بيتلز`** · ٤ صفحات `/c/`
منشورة بمحتوى نظيف مكتوب يدوياً (عناوين بنيّة الاستعلام، صفر فبركة، FAQ) · `blog_bridge`
١٦ صفّاً `fnp-*` → `{فيرنز اند بيتل}` · `next build` كامل `EXIT=0` (2121/2121) · FAQPage +
BreadcrumbList يُصيَّران على كل عيّنة.

**عُطلان أُصلحا (web `44d73c8`):**
1. **العنقود كان يتيماً من العمود الفقري** — كل عنقود سابق له سطر في هَب التوفير + مقال
   best-stores؛ FNP بلا **أي** رابط وارد في `blog.ts` (فقط أشقّاؤه الـ١٦). أُضيف سطر FNP
   للهَبين (زاوية الورد/الهدايا، جوار مرايا).
2. **فئة خاطئة** — الـ١٦ استخدمت `category: 'هدايا ومناسبات'` (الاسم الكنسي، **ليس مفتاح
   `CATEGORY_ALIASES`**) فسقطت لـ`savings-guides`. HTML المُصيَّر أكّد: `/blog/category/gifts`
   صفر FNP، `savings-guides` كلها الـ١٦. غُيّرت لـ`'هدايا'` (مفتاح الاسم الذي تستخدمه مرايا).
   الآن gifts=16، savings-guides=0.

⚠️ **تحقّق مفتوح للمالك:** أسماء ماركات الشوكولاتة في صفحتَي `/c/` والمقالات (باتشي/جوديفا/
آني/داني/ميونخ/كانزل/فينشي) — من لقطاتك لا من `store_bio`. تأكّد أنها ظهرت فعلاً على fnp.sa.

⚠️ `blog_bridge --write` بعد تعديلات اليوم **معلّق** (مصنّف الهارنس يحجبه) — الإضافة طفيفة
(أسطر هَب بلا `/store/` جديد عدا FNP في best-stores الذي يتجاوز سقف ٦ متاجر أصلاً).

## ملاحظة أداة

`run_migration.py` كان يـcrash على طبع ✅ بترميز ويندوز cp1252 **بعد** تطبيق SQL
(autocommit) فيبلّغ فشلاً كاذباً — أُصلح بفرض UTF-8 على stdout/stderr.

يكمّل [[blog_maxfashion_puma_clusters]] · [[content_guardrails_playbook]] · [[feedback_verify_catalog_before_claim]].
