---
name: brand-logo-ticket-2026-08
description: اللوقو الجديد (تذكرة + DP + نبض) اعتُمد ٢٠٢٦-٠٨-١٢، وطقم ٨٧ ملفاً بُني منه — ولماذا كذبت عليّ شبكة عيّنات خشنة فأبلغت عن عيب غير موجود
metadata:
  type: project
---

**اعتُمد ٢٠٢٦-٠٨-١٢.** لوقو جديد بديلاً عن `logo.png` القديم: **تذكرة كوبون مقفولة**
(قضمتان جانبيتان + تقطيع رأسي منقّط) بداخلها مونوجرام `DP` وخط نبض `#10B981`،
و«نبض الصفقات». حبر موحّد `≈#141C31`. وُلِّد في جيمناي عبر برومبت تكراري.

**الماستران النهائيان** — كلاهما مرّ تدقيقاً **مقيساً** لا بصرياً:

- `Gemini_..._w0ut2v….png` (٢٨٠٠×١٥٠٤، أبيض) — الكامل. تذكرة `x 0.199→0.807`،
  مقفولة (`y 0.237→0.834`)، قضمتان `0.42–0.58h` بـ٥٨ صفاً لكل واحدة، تقطيع ٧٧٪
  بـ٦ شُرَط، كتلة نصّية واحدة.
- `Gemini_..._mx2hdz….png` (٢٨٠٠×١٥٠٤، فحمي) — الأفقي المعكوس، الاسم يسار والتذكرة
  يمين. تذكرة `x 0.440→0.916`، مقفولة، قضمتان `0.44–0.56h` بـ٤٩ صفاً، تقطيع ٧٦٪.
  **النبض بقي أخضر عمداً** — `#10B981` على `#0F172A` تباينه ~٦٫٥:١، فالـknockout
  الأبيض كان سيرمي لون العلامة بلا مقابل.

**الطقم:** `C:\Users\PC\Desktop\dpk-logo\KIT\` — **٩٧ ملفاً** + `README.md` بخريطة كل
ملف لوجهته. المجلدات: `logo-full-{light,dark,transparent}` · `lockup-horizontal-dark` ·
`icon-square-*` · `avatar-circle` · `boards` · `web/favicon.ico`. لم يُطبَّق على الريبوين بعد.

## أرقام قِيست على اللوقو القديم — كلها عطب حيّ

- `<link rel="icon" href="/logo.png">` والملف **١٠٢٤×٥٠٦** ⇒ الفافيكون **١٦×٨ بكسل**.
- `og:image` يعلن `1200×630` والملف الحيّ ١٠٢٤×٥٠٦.
- **صفر بكسل شفاف** — خلفية كريم مطبوخة؛ لذلك بلاطة بيضاء تحت اللوقو في `Header.tsx:56`.
- ملف واحد يخدم ٩ أسطح، منها `lib/cloudinary.ts:9` بديلاً لشعار متجر ناقص.

## قاعدتان مقيستان تحكمان أي لوقو هنا

- **التذكرة بإطارها تموت عند 32px** — الإطار يأكل الارتفاع فيبقى الـDP ٤٠٪ منه.
  فالفافيكون والأفاتار من **DP وحده**، لا من اللوقو الكامل.
- **`DEAL PULSE KSA` يصير لطخة تحت 64px.**
- الهيدر يرسم «نبض الصفقات» نصّاً أصلاً ⇒ ضع الأيقونة لا اللوقو بالاسم، وإلا تكرّر الاسم.

## 🔴 الدرس: شبكة عيّنات خشنة تُنتج عيباً وهمياً

أبلغتُ المالك أن التذكرة **مفتوحة من الأسفل**، بدليل «صفر بكسل داكن» عند
`0.70h, 0.73h … 0.94h`، وأن العمود الأيسر «ينتهي عند 0.664h». **كلاهما خطأ قراءة:**
الحدّ السفلي موجود عند **`0.675h → 0.697h`** — بين عيّنتين من شبكتي — والعمود انتهى
لأن **قوس الزاوية** يبدأ هناك، لا لأن الحاوية مفتوحة. المسح بدقّة `0.001` كشفها.
المالك كان محقاً وأنا الغلطان.

**القاعدة:** غياب إشارة في شبكة خشنة ≠ غياب الشيء. قبل الإبلاغ عن عيب بنيوي في صورة،
امسح المدى المشتبه بدقّة ناعمة، ولا تستنتج انقطاعاً من عمود واحد قد يكون طرف منحنى.
يربط [[feedback_mirror_audit]].

## فخّ خطوط عربية (يعضّ الكود لا التصميم)

`Almarai` و`Tajawal` و`Dubai` **لا تحوي** `U+FE95` (ت مفردة) و`U+FE8D` (ا مفردة)،
فأي كود يستخدم `arabic_reshaper` معها يطبع **مربّعات فارغة**. `IBM Plex Sans Arabic`
وحدها كاملة الأشكال. الحل: بديل عبر `unicodedata.decomposition` يرجّع الحرف لأصله.
يخصّ `api/social/content_reels.py` و`api/social/reels_render.py`.

**وأهم فخّ في المولّدات:** جيمناي **لا يُخرج قناة ألفا** — يرسم شطرنجاً *داخل* الصورة.
قِستها: `transparent=0.0%` في ست صور متتالية. أي «خلفية شفافة» منه تحتاج keying يدوياً.

Related: [[web_coupon_visual_identity]] [[website_design_preferences]] [[brand_face_reels]]
