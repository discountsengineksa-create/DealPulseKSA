# خطّ أساس Drift — www.dealpulseksa.com

**التُقط:** ٢٠٢٦-٠٨-١١ · `03:07:21Z` (٠٦:٠٧ الرياض) · `baseline_id: 10` للأولى
**السبب في هذا التوقيت بالذات:** إصلاح الكاش شُحن قبل ساعات، وهذه اللقطة هي ما يجعل أثره **قابلاً للقياس** لاحقاً بدل أن يبقى انطباعاً.

## ما التُقط — سبعة روابط، واحد لكل قالب

| الرابط | القالب | الحالة |
|---|---|---|
| `/` | الرئيسية | ✅ |
| `/calendar` | الموسمي (أعلى CTR: ٢.٠٤٪) | ✅ |
| `/store/بيلاس` | متجر — **مشمول بإصلاح الكاش** | ✅ |
| `/store/نمشي` | متجر — أعلى ظهوراً (٩٧٨) | ✅ |
| `/category/تطبيقات` | قسم — **مشمول بإصلاح الكاش** | ✅ |
| `/c/كود-خصم-bellas-2026` | صفحة كود (صفر نقرة عبر ٣٥ صفحة) | ✅ |
| `/blog/school-records-guide-saudi` | أنجح مقال (٥ نقرات) | ✅ |

**سبعة من سبعة نجحت.** كل واحدة تحمل بصمة HTML وعنواناً وcanonical وrobots وH1 وكود حالة.

## التحقّق — الحلقة مغلقة لا مفتوحة

لم أكتفِ بالالتقاط، بل شغّلت المقارنة فوراً للتأكّد أن النظام يقرأ ما كتب:

```
drift_compare → "No drift detected. Page matches baseline."
17 قاعدة مفحوصة · 0 مُفعّلة · 0 حرجة
```

⚠️ **موضع التخزين لم أحدّده.** ليس تحت `%LOCALAPPDATA%\claude-seo` ولا `data/` بالبلَغن
ولا `~/.config/claude-seo`. تديره الأداة داخلياً، و`drift_history` و`drift_compare` يقرآنه
بلا مشكلة — **فهو يعمل، لكنه غير مُتتبَّع بـgit ولا ينتقل للجهاز الثاني.** أعِد الالتقاط هناك
عند الحاجة بدل افتراض المزامنة.

## كيف تقارن لاحقاً

```powershell
$rt="C:\Users\PC\.claude\plugins\cache\agricidaniel-claude-seo\claude-seo\2.2.4\scripts\runtime.py"
python $rt run drift_compare.py <URL>     # الحالة الآن مقابل خطّ الأساس
python $rt run drift_history.py <URL>     # كل اللقطات المخزّنة
```

## ما يستحق المراقبة — مشتقّ من التغيير المشحون

**١. رؤوس الكاش — تراجُع لو حدث.** القياس قبل/بعد كان:
`x-vercel-cache: MISS` + `private, no-cache, no-store` ⇐⇒ `PRERENDER` + `public, max-age=0`.
أي عودة لـ`MISS` على `/store` أو `/category` تعني أن انسحاباً ديناميكياً جديداً تسلّل —
والسببان المعروفان: قراءة `searchParams` في مكوّن خادم، أو `generateStaticParams` سقطت.

**٢. مركز و CTR على `/store` و`/category`.** خطّ الأساس المقيس (٢٨ يوماً حتى ٢٠٢٦-٠٨-١٠):

```
/store     6,693 ظهور ·  17 نقرة · CTR 0.25٪
/category    815      ·   4      · CTR 0.49٪
```

**٣. الفهرسة.** الصفحات صارت مبنيّة مسبقاً (`PRERENDER`) — راقب أن العدد المفهرَس لا ينقص.

⚠️ **ولا تقرأ آخر يومين في GSC** ([[seo_audit_tools_trust]])، **ولا تجمع صفوف الاستعلامات**
لأن ٤١٪ من الظهور و٧٤٪ من النقرات محجوبة بعتبة الخصوصية — استخدم `--dimensions date` للإجماليات.

## نافذة القياس

**٤ أسابيع على الأقل قبل أي تغيير آخر على `/store` أو `/category`** — وهذا بالضبط سبب تأجيل
هجرة `%20` (§١٠ في `technical.md`). تكديس تغييرين على قالب واحد يُفقد النسبة: لا تعرف أيّهما
حرّك الرقم. القياس الأول المفيد بعد **٢٠٢٦-٠٩-٠٨**.
