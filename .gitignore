venv/
__pycache__/
*.pyc
*.pyo
.env
.streamlit/secrets.toml
dump.bin
*.dump
.vscode/
.idea/
.DS_Store
Thumbs.db

# ─── إضافات أمنية ───────────────────────────────────────────────────────────
# أي تصدير قاعدة بيانات أو CSV ببيانات حية لا يُلتزم
db_export.sql
*-export.sql
data-*.csv
import_log.txt
# معاينة جسر المدوّنة — مشتقّة من blog.ts، تُولَّد محلياً قبل الملء
data/blog_bridge_preview.json

# ملفات السياق العربية الكبيرة (ملاحظات تطوير قد تحتوي مفاتيح/أسرار)
Full_Context_Memory.txt
الداشبورد الاخير.txt
النهائي وبداية التصميم.txt
كود الداشبورد كامل.txt
رابط الموقع.txt
weeks plan.txt
Transfer_Package/

# ─── ملاحظة ──────────────────────────────────────────────────────────────────
# migration_*.sql مستثناة (تظل في git) لأن المهاجرات لازم تُتعقّب.
# لو غضضت `*.sql` بالكامل تخسر تتبّع المهاجرات.

# ─── ملفات تشخيصية مؤقتة (من curl/diagnose) ─────────────────────────────────
openapi_dump.json
openapi_v2.json
site_home.html
site_home_full.html
robots.txt
sitemap.xml

# ─── ملفات مؤقتة/نفايات (logs, locks, ملاحظات طلبات) ────────────────────────
BOOTEX.LOG
*.log
.claude/scheduled_tasks.lock
.claude/*.lock
طلب اصلاح الميني ويب.txt
طلب اصلاح الميني وي ب.txt


# dashboard boot-test log (§4.9)
dash_test.log
