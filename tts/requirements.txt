coqui-tts>=0.24.2
# coqui-tts's tortoise autoregressive layer imports
# transformers.pytorch_utils.isin_mps_friendly, which exists ONLY in the
# window transformers 4.41–4.49. It was added in 4.41 and removed in 4.50+
# (MPS friendliness became implicit). Pin the safe middle.
transformers>=4.44,<4.50
# transformers 4.4x hard-pins tokenizers 0.20.x. If pip already resolved
# tokenizers to a newer major (0.21+/0.22+) from an older transformers install,
# it won't downgrade on its own — pin it too.
tokenizers>=0.20,<0.21
torch>=2.1
torchaudio>=2.1
soundfile>=0.12.1
numpy>=1.24
lameenc>=1.7.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9
