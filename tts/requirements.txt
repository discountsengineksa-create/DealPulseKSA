coqui-tts>=0.24.2
# Coqui-tts is unmaintained (2024) and its metadata under-pins the whole
# HuggingFace stack. Empirically-safe pin set for XTTS v2 on Windows:
#   - transformers must be in [4.41, 4.50): 4.41 added isin_mps_friendly,
#     4.50 removed it. XTTS's tortoise layer needs it.
#   - transformers 4.46 hard-requires tokenizers 0.20.x and
#     huggingface-hub >=0.23.2,<1.0.
# All three must be pinned together — bumping one alone breaks the others.
transformers==4.46.3
tokenizers==0.20.3
huggingface-hub>=0.23.2,<1.0
torch>=2.1
torchaudio>=2.1
soundfile>=0.12.1
numpy>=1.24
lameenc>=1.7.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9
