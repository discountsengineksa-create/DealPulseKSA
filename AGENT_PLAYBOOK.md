# AGENT_PLAYBOOK — كيف تشتغل على DealPulse بذكاء ووعي

> **اقرأ هذا الملف كاملاً قبل أول تعديل في كل جلسة.** هذا ليس توثيقاً للكود — هذا
> **طريقة التفكير** التي يتوقعها صاحب المشروع. الفرق بين حلّ المشكلة من أول محاولة
> وبين ٦ محاولات فاشلة هو المنهج في هذا الملف، وليس الذكاء الخام.

مالك المشروع مهندس خبير (٢٠+ سنة)، يقيّم الشغل **بالنتيجة النهائية لا بالجهد**،
ويكره الفلسفة والخيارات والأساسيات. يريدك: تقرأ، تشخّص الجذر، تنفّذ، تتحقّق. بالعربي
في كل ما يخص المستخدم؛ الإنجليزي للكود وأسماء SQL فقط.

---

## ١) القاعدة الذهبية: شخّص الجذر، لا تعالج العَرَض

**القصة التحذيرية (حصلت فعلاً):** محرّك صوت محلي فشل تثبيته. الرسالة كانت
`No module named 'lameenc'`. Claude آخر حاول ٦ مرات: كل مرة يثبّت `transformers`
نسخة أقدم (`4.49`, `<4.50`, يثبّت الحزمة الكاملة...) — كلها **علاج للعَرَض**. فشل
كل مرة لأن السبب الحقيقي كان **سلسلة من ٥ طبقات** والرسالة الظاهرة كانت **كاذبة**:
`pip` كان يفشل في حلّ التبعيات (`encodec` بلا دعم Python 3.13) **قبل** أن يصل
`lameenc` أصلاً.

**الدرس المُلزِم:**
- **رسالة الخطأ الأولى غالباً كاذبة أو سطحية.** لا تصلح ما تقوله الرسالة حرفياً —
  اسأل *لماذا* ظهرت. شغّل الأمر الفعلي وشوف الفشل الحقيقي (هنا: `pip install
  --dry-run` كشف `ResolutionImpossible: encodec`).
- **لو حلّك الأول فشل، لا تكرّر نفس نوع الحل بمعامل مختلف.** التكرار = ما فهمت
  الجذر. ارجع خطوة، وسّع التشخيص.
- **اقرأ الكود/التبعية الحقيقية قبل ما تفترض.** فتحنا ملف `autoregressive.py`
  ورأينا الاستيراد المكسور بأعيننا بدل التخمين.
- **حُلّ طبقة طبقة بالتحقق بينها:** كل إصلاح كشف الطبقة التالية (encodec → numba
  cache → isin_mps_friendly → torchcodec → تنزيل النموذج). ٥ إصلاحات، كل واحد
  مُتحقَّق منه قبل التالي.

## ٢) تحقّق end-to-end — لا تدّعي النجاح بلا دليل

- "ثبّت الحزمة" ليس نجاحاً. **شغّل الشيء وأنتج مخرَجاً حقيقياً** (هنا: ولّدنا ملف
  MP3 فعلي وتأكدنا حجمه = bitrate × المدة، أي صوت حقيقي لا صمت).
- إذا فشلت الاختبارات، قُلها بصراحة مع المخرَج. لا تجميل.
- التغييرات الحسّاسة أو الخارجية: أكّد قبل التنفيذ ما لم يكن هناك إذن دائم صريح.

## ٣) الصراحة عن السقف قبل البناء

المستخدم رفض ميزة اشتغلت هندسياً (٠/١٠٠) لأن **المخرَج** (جودة الصوت) كان دون
تطلّعه. **الدرس:** لو الأداة المختارة سقفها أقل مما يريد، **قُل ذلك بصوت عالٍ ومن
البداية** قبل ما تستثمر جهداً في تلميعها. عامِل قيود مثل "محلي/مجاني/بلا API"
كوسيلة قابلة للمقايضة عند كشف السقف، لا كقيد مقدّس. راجع
`Claude_Memory/feedback_output_over_engineering.md`.

## ٤) الذاكرة هي دماغك المشترك — استخدمها وحدّثها

- عند بداية كل جلسة تُحمّل `Claude_Memory/MEMORY.md` (فهرس سطر لكل ذاكرة). **اقرأ
  الفهرس، وافتح الملف المعني قبل أي عمل متعلق به.**
- الذاكرة تتزامن بين الجهازين عبر **git junction** (`Claude_Memory/` داخل الريبو +
  junction على كل جهاز). أي `git pull` يجيب أحدث ذاكرة. أي `git push` ينشرها.
- **حدّث الذاكرة** بعد أي قرار/إصلاح/تفضيل غير بديهي. ملف واحد = حقيقة واحدة، مع
  frontmatter، وسطر مؤشّر في MEMORY.md. راجع `Claude_Memory/memory_sync_junction.md`.
- الذاكرة تعكس ما كان صحيحاً وقت كتابتها — **تحقّق أن الملف/الدالة ما زالت موجودة**
  قبل التوصية بها.

---

## ٤.٥) انضباط الاستهلاك — أنجز أكثر بتوكِنز أقل

**معيار على كل agent يعمل هنا (كلاهما — لا أحد فوق التدقيق).** الحكم يكون بالـ **trace الخام** لا بالادّعاء: خذ أي جلسة استهلكت ≥٥٠٠٠ توكن واكتب الاستدعاءات بالترتيب مع سبب كلٍّ — الـ trace يكشف ما يخفيه الملخّص (قراءة ملف كامل؟ `grep` خام بدل `Grep`؟ تكرار نفس المحاولة بمعاملات مختلفة؟). لو تبيّن أن agent آخر أكفأ بالأرقام، **هو المرجع وتتعلّم من أسلوبه** — التواضع بالبيانات لا بالمنصب. الهدر ليس التفكير، بل **العمل المتكرر والزائد**. القواعد:

1. **اقرأ الذاكرة أولاً، لا تعيد الاكتشاف.** أكبر هدر: استكشاف كودٍ موثَّق أصلاً في `Claude_Memory/`. تأكّد أن الـ **junction معمول على هذا الجهاز** (وإلا تقرأ صفر ذاكرة وتبدأ بارداً كل جلسة = هدر ضخم). راجع الذاكرة قبل `grep` عشوائي.
2. **لا تقرأ ملفاً ضخماً كاملاً أبداً.** `blog.ts` **٥٦٠٠٠ سطر**، `dashboard.py` **٢٨٠٠+ سطر**. اقرأها كاملة = كارثة توكِنز. بدلاً: `Grep` بأرقام أسطر → ثم `Read` بـ `offset/limit` حول الإصابة فقط.
3. **المحاولة الواحدة الصحيحة > ٦ محاولات فاشلة.** حلقة إعادة المحاولة بلا تشخيص هي **أكبر بالوعة توكِنز**. شخّص الجذر (قسم ١) ثم نفّذ مرّة.
4. **استدعاءات الأدوات المستقلة بالتوازي**، دفعة واحدة — لا بالتسلسل.
5. **لا تعيد التحقق مما ثبت.** لو تأكّدت من شيء، لا تكرّره. لا تعيد قراءة ملف عدّلته للتو (أداة التعديل تفشل لو ما نجح التغيير).
6. **أدوات مخصّصة لا شِل خام:** `Grep`/`Glob` بدل `grep -r`/`find` على الشجرة (أسرع وأرخص وما يخنق الذاكرة).
7. **عند الانسداد الحقيقي: توقّف واسأل سؤالاً نصياً قصيراً** — لا تثرثر ٢٠ استدعاءً تخمينياً.

**القاعدة:** كل استدعاء أداة له سبب. لو ما تعرف ليش تشغّله، لا تشغّله.

---

## ٤.٦) بروتوكول التدقيق الذاتي — مضاد الهبد (إلزامي قبل كل شحنة)

**الهدف:** تمسك خطأك بنفسك **قبل** ما يمسكه المُراجِع. الموديل ليس عذراً للهبد؛ الهبد = **ادّعاء/شحن بلا إثبات مُشغَّل**. القاعدة الحاكمة: **لا تُرسل نتيجة إلا ومعك مخرَج أمر يثبتها.** لو ما تقدر تثبتها بأمر، فهي ادّعاء لا حقيقة.

### أ) قبل الفعل — شخّص بصوت عالٍ
اكتب لنفسك سطراً واحداً: **«السبب الجذري هو __، وسأثبته بأمر __».** لو فشل شيء، **لا تصلح الرسالة الظاهرة** — شغّل الأمر الكاشف (`--dry-run`، اقرأ الملف الفاشل). محاولة واحدة مشخّصة > ستّ تخمينات (قصّة 01ed10b).

### ب) قبل شحن مقال — سكربت واحد، شغّله مرّة واعرض مخرجاته (لا ارتجال = لا تأخير)
```bash
S=<slug-الجديد>
START=$(grep -n "slug: '$S'" lib/blog.ts | cut -d: -f1)
NEXT=$(awk -v s="$START" 'NR>s && /^[[:space:]]*slug:/{print NR;exit}' lib/blog.ts)
STOP=${NEXT:-$(wc -l < lib/blog.ts)}          # نطاق أمتن: من الـ slug حتى slug التالي
A(){ sed -n "${START},${STOP}p" lib/blog.ts; } # جسم المقال
echo "١) روابط سليمة:"; A|grep -oE '/blog/[a-z0-9-]+'|sort -u|while read t; do grep -q "slug: '${t#/blog/}'" lib/blog.ts && echo "  ✓ ${t#/blog/}"||echo "  ✗ مكسور ${t#/blog/}"; done
echo "٢) غير يتيم (≥2): $(grep -c "$S" lib/blog.ts)"
echo "٣) فبركة ريال (=0): $(A|grep -cE '[٠١٢٣٤٥٦٧٨٩0-9]+ ?(ريال|SAR|﷼)')"  # عربي-هندي + لاتيني
echo "٤) بنية: جدول=$(A|grep -cE '^\|.*\|') FAQ=$(A|grep -c 'أسئلة شائعة') إفصاح=$(A|grep -c 'إفصاح')"
BL=$(A|grep -oE '/blog/[a-z0-9-]+'|sort -u|wc -l); ST=$(A|grep -oE '/store/[^)\"]+'|sort -u|wc -l)
echo "   روابط: blog=$BL store=$ST المجموع=$((BL+ST)) (٩–١٣)"
echo "٥) backtick body (=1): $(A|grep -c 'body: `')"
```
**كل الفحوص في تمريرة واحدة.** لو أي واحد سقط → أصلحه قبل الشحن. لا «خلصت» إلا والكل أخضر. (اكتب المقال + روابط فك اليُتم في **نفس تمريرة الـ Edit** لا edits متفرّقة.)

### ج) قبل ادّعاء أي إنجاز — تحقّق في السجل لا في الذاكرة
«أنا سويت/حلّيت» يحتاج `git show`/`grep`، لا ثقة بالذاكرة. (تعلّمها Claude 4.8 لمّا ادّعى فضلاً بلا فحص ملكية — [[feedback_mirror_audit]].)

### د) قبل شحن أي تقرير/تركيبة فيها أعداد — عُدَّ حيّاً لا تقتبس من الذاكرة
كل عدد يقبل العدّ (مقالات/مايقريشنز/صفحات/متاجر/routers/commits) = **يُعَدّ حيّاً قبل الإرسال**. الذاكرة تسرد **الخصائص** (بنية، قرار، سبب) لا **الأعداد اللحظية** (تتغيّر أسبوعياً). **التقرير الذي فيه أرقام قابلة للتحقّق أخطر من تعديل الكود** لأنه يُقرأ كحقيقة موثّقة ويؤسِّس عليه المالك قرارات.

**مسح ما قبل الإرسال — علم أحمر لأي «~/حوالي/تقريباً/عدد صريح» في المسودّة:** إمّا عدّه بدقّة أو احذف الرقم. الجدول المرجعي:

| الفئة | الأمر |
|---|---|
| مقالات المدوّنة | `grep -cE "^\s*slug:" lib/blog.ts` (في `dealpulseksa-web`) |
| مايقريشنز | `Glob migration_*.sql` (احسب الفجوات، مثال 021+042 مفقودتان) |
| صفحات الداشبورد | `grep -cE '^(if\|elif) page ==' dashboard.py` |
| Routers | `grep -cE 'include_router' bot_app.py` |
| Commits منذ تاريخ | `git log --oneline --since=... \| wc -l` |
| جداول DB / متاجر | SQL على Railway (بإذن قراءة، §٥) |

**الفخّ الذي كسر تقرير 2026-07-07:** جمعتُ «٢٢٧ + ~٥٥ + ~٢٠٠ = ~٤٨٢ مقال» + «~٦٣ مايقريشن» من ذاكرة جلسات سابقة. الحيّ ٦٥٠ + ٦١. أمسكها ولد الخاله بعدّ سطرَي `grep` قبل ما أدفع. **العدّ الحيّ أسرع من الاعتذار.**

### حلقة التدريب (تتصاعد الثقة بالأداء)
- **مرحلة ١:** تشغّل كل الفحوص وتعرض مخرجاتها، والمُراجِع يعيد فحص كل شحنة.
- **مرحلة ٢:** تدقّق ذاتياً، المُراجِع يفحص عيّنة.
- **مرحلة ٣:** موثوق — فحص دوري فقط.
التقدّم **يُكتسب بأرقام نظيفة متتابعة**، لا بالوقت.

---

## ٤.٧) الانضباط عبر كل المجالات (بوت/ميني/API/داشبورد/ويب/سوشيال)

نفس المبدأ في كل مكان: **لا تُرسل نتيجة بلا مخرَج أمر يثبتها.** لكن لكل مجال «فحص ما قبل الشحن» الخاص + خطّه الأحمر. **اقرأ العمود قبل ما تلمس المجال:**

| المجال | افحص قبل الشحن | 🚩 الخط الأحمر |
|---|---|---|
| **الموقع/صفحات هبوط/نيش** | §٤.٦ كامل (روابط حقيقية، غير يتيم، صفر فبركة، بنية) + كوبونات/خصومات **حقيقية من DB لا مخترعة** + `publish_channels` يُحترم + brand-safety/YMYL ([[content_guardrails_playbook]]) | لا صفحات رقيقة مكرّرة؛ السلطة = باكلينك لا كمّ صفحات ([[seo_indexation_status]]) |
| **داشبورد (dashboard.py)** | `python -m py_compile dashboard.py` بعد كل تعديل؛ لا تكسر سلسلة `if/elif`؛ فخّ `store_tags` (نص لا مصفوفة)؛ العدّادات الحيّة `total_*` لا الميتة؛ نمط `rollback`؛ SQL إنجليزي ثم عربي في Python | §٥: **لا كتابة DB بلا إذن صريح للعملية** |
| **البوت / bot_app** | — | 🔒 §٥: **مجمّد — لا تعديل إطلاقاً إلا بإذن صريح لكل تغيير.** «طوّر البوت» ≠ عدّل البوت. اقترح واستأذن، لا تلمس |
| **ميني/API (`api/`)** | استيراد/تشغيل نظيف؛ لا تكسر endpoint قائم؛ احترم SSOT (`user_favorites`)؛ اقرأ `.env` قبل أي اقتراح بنية ([[feedback_check_env_first]]) | لا schema/كتابة DB بلا إذن + نسخة |
| **سوشيال/تواصل** | brand-safety (الفئات المستثناة)، صفر فبركة، **White-Hat فقط** ([[seo_white_hat_only]])؛ احترم قيود المزايدة على البراند ([[affiliate_ppc_brand_restrictions]]) | لا انتحال، لا Black-Hat، لا مزايدة مدفوعة على اسم براند |
| **تقارير/تركيبات (Synthesis)** | §٤.٦-د كامل: كل عدد في المسودّة يُعَدّ حيّاً (جدول الأوامر أعلاه). أي «~/حوالي» علم أحمر — عُدَّه أو احذفه. الاستشهاد بأسطر/ملفات محدّدة حرفياً لا تقريباً. | ادّعاء رقم قابل للتحقّق من الذاكرة = تلفيق موثّق (وقعتُ فيه 2026-07-07 — ٤٨٢ ادّعاء، ٦٥٠ الحقيقي) |

**قاعدة عابرة للمجالات:** لو ما تعرف أثر تغييرك على مجال آخر — **اقرأ أولاً** (الذاكرة + الكود)، لا تخمّن. وحاجز الجهازين ([[content_guardrails_playbook]]): لا تلمس قسم الجهاز الآخر.

## ٤.٨) طول البال + فهم المالك حتى لو كلامه ناقص

المالك خبير، سريع، وأحياناً **يكتب ناقصاً أو غير مرتّب** — واجبك تفهم **النيّة** لا الحرف:
- **اقرأ المقصد من السياق + الذاكرة**، لا من ظاهر الكلمات. لو طلب «طوّر X» غامضاً، استنتج من [[content_guardrails_playbook]] وحالة المشروع ما هو التطوير المفيد فعلاً.
- **لو ناقص لكن يمكن استنتاجه** → نفّذ على أوضح تفسير، واذكر افتراضك في سطر. **لو ناقص ولا يمكن استنتاجه** → سؤال نصّي **واحد** قصير، لا قائمة خيارات ([[feedback_no_philosophy]]).
- **طول البال:** لا تتوتّر ولا تثرثر عند الغموض أو الخطأ. شخّص بهدوء، جرّب أضيق تفسير، تحقّق. المحاولة الواحدة المدروسة تهزم ستّ محاولات متسرّعة — دائماً.
- **لا تجامل، ولا تتذرّع بالموديل.** لو أخطأت، اعترف وأصلح بمخرَج. لو المالك غير راضٍ عن نتيجة، لا تدافع بقائمة إنجازات — صحّح المخرَج.
- **الثبات الانفعالي = ميزة تقنية:** الوعي الهادئ يرى الطبقة الخامسة؛ التسرّع يرى الرسالة الأولى فقط.
- **الإيجاز جودة، لا سرعة:** اقصّ الحشو (تكرار، أقسام زائدة) لأنه يُضعف المقال — لا لتوفير دقائق. **كل قسم يستحق مكانه؛** المقياس «صفر حشو» لا «عدد أسطر». ولا تقايض عمقاً حقيقياً بالسرعة — المالك يقيّم بالمخرَج أولاً ([[feedback_output_over_engineering]]).

---

## ٤.٩) بروتوكول أمان تعديل الداشبورد (إلزامي — `main` = إنتاج Railway)

`dashboard.py` ملف واحد ~٢٨٠٠ سطر بسلسلة `if/elif page ==` (~٣٥ صفحة). كسرة واحدة = **الداشبورد كله يسقط**، ولأن الدفع لـ `main` نشر إنتاجي مباشر، **دفع داشبورد مكسور = إنتاج مكسور.** لذا: **ممنوع الدفع بلا إثبات إقلاع محلي.**

```bash
# ٠) خطّ أساس قبل التعديل — سجّل عدد الصفحات
BEFORE=$(grep -cE '^(if|elif) page ==' dashboard.py); echo "صفحات قبل: $BEFORE"
#    عدّل داخل بلوك elif المستهدف فقط. لا تلمس بلوكات الصفحات الأخرى ولا قائمة الراديو (إلا بقصد).

# ١) يُصرّف؟ (يمسك أخطاء الصياغة = أشهر طريقة «تدمير»)
python -m py_compile dashboard.py && echo "✓ صرف" || echo "✗ صرف مكسور — أصلح أو ارجع"

# ٢) السلسلة سليمة؟ (نفس العدد ما لم تقصد إضافة/حذف صفحة؛ اختلاف = بلوك انكسر/انحذف)
AFTER=$(grep -cE '^(if|elif) page ==' dashboard.py); echo "صفحات بعد: $AFTER  (يجب = $BEFORE)"

# ٣) يُقلع بلا Traceback؟ (يمسك أخطاء مستوى الوحدة)
venv/Scripts/streamlit.exe run dashboard.py --server.headless true >dash_test.log 2>&1 &
sleep 12
(netstat -ano | grep -q ":8501.*LISTENING") && ! grep -qi "traceback" dash_test.log \
  && echo "✓ أقلع نظيفاً على 8501" || { echo "✗ إقلاع فشل:"; tail -8 dash_test.log; }
#    أوقف اختبار الإقلاع بعده (taskkill على PID المنفذ 8501).
```

**اقفال القرار:** ادفع **فقط** لو الثلاثة خضراء (صرف ✓ + العدد ثابت + إقلاع ✓). لو أي واحد سقط → **`git checkout dashboard.py`** (ارجع) وشخّص، **لا تدفع أبداً داشبورد لم يُقلِع**.

**تنبيه:** الإقلاع لا يختبر منطق صفحة بعينها (streamlit ينفّذها عند الزيارة). لتغيير منطقي/استعلام: تحقّق من الاستعلام مستقلاً (شغّله للقراءة على DB) — واحترم فخاخ §٤.٧ (`store_tags` نص، العدّادات الحيّة، `rollback`) و**§٥ لا كتابة DB بلا إذن**.

---

## ٥) القواعد الحمراء للمشروع (كسرها = كارثة)

| القاعدة | التفاصيل |
|---|---|
| 🔓 **البوت مفكوك (2026-07-07)** | التعديل مسموح ضمن بروتوكول الشراكة (إعلان → تحقّق → إثبات → تعلّم). النسخة الأصلية محفوظة بتاغ `bot-locked-2026-06-10` كملاذ استرجاع. **الفكّ لا يبطل حائط DB writes ولا حائط `main=prod`.** راجع `bot_frozen_lock.md`. |
| 🚫 **لا كتابة DB بلا إذن** | أي `INSERT/UPDATE/DELETE` على `discounts_engine` يحتاج إذناً صريحاً **للعملية المحددة**. "يلا نبدأ" ليس إذن كتابة. القراءة حرة. راجع `feedback_no_db_writes_without_permission.md`. |
| 🔵 **main = إنتاج Railway** | الدفع لـ `main` = نشر إنتاجي مباشر. المستخدم يريد **commit+push تلقائي عند إنهاء المهمة** بلا سؤال، لكن انتبه لأثر النشر. راجع `feedback_always_publish.md` + `railway_deployment.md`. |
| 🔄 **اسحب قبل، ادفع بعد** | المستخدم يشتغل على **جهازين**. `git pull` بداية الجلسة، `git push` بعد كل تغيير. لا تترك شغلاً محلياً أبداً، لا تعمل stash. راجع `git_sync_workflow.md` + `feedback_always_push.md`. |
| 🌐 **ريبو الويب منفصل** | `dealpulseksa-web` ريبو مستقل، قد يكون متأخراً عن ريموته حتى لو الريبو الرئيسي متزامن. افحصه/اسحبه منفصلاً. راجع `reconcile_web_repo_separately.md`. |
| ⚪ **البيانات الحقيقية فقط** | لا فبركة، لا كود ميّت، لا تحسين استباقي. كل سطر موصول ومبرّر ببوتلنك فعلي. المدينة الحقيقية من `web_users.city`/`action_logs.city` لا `bot_users` (مفبركة). راجع `feedback_no_dead_code.md` + `data_trust_geo_device.md`. |
| ⚡ **نفّذ، لا تُفلسِف** | لا أسئلة متعددة الخيارات، لا مقدمات تخطيط. ابدأ بأوضح جزء، واسأل سؤالاً نصياً قصيراً واحداً فقط عند الانسداد الحقيقي. راجع `feedback_no_philosophy.md` + `feedback_senior_engineer.md`. |
| 🩺 **عند "اشتغل قبل تغييرك"** | افحص **diff الخاص بك أولاً**، لا تخمينات ولا كود استباقي. راجع `feedback_regression_audit.md`. |

---

## ٦) فخاخ الكود التي ستعضّك (من CLAUDE.md + خبرة مؤلمة)

- **`store_tags` عمود `text` وليس `text[]`** لكن البيانات مكتوبة بصيغة مصفوفة
  `'{a,b,c}'`. `unnest`/`ANY(store_tags)` **تفشل وقت التشغيل**. حوّل أولاً:
  `string_to_array(trim(both '{}' from COALESCE(store_tags,'')), ',')`. للبحث:
  `store_tags ILIKE '%tag%'`.
- **العدّادات الحيّة** هي `master.total_link_clicks` / `master.total_coupon_copies`
  فقط. الأعمدة `link_clicks`/`copy_clicks`/`click_count`/`total_clicks` **ميتة/بايتة**
  — لا تقرأها.
- **حالة المعاملة:** صفحات القراءة تنادي `conn.rollback()` أو `autocommit=True` في
  الأعلى لتنظيف حالة معاملة مُجهَضة. **نمط مقصود — لا تحذفه.**
- **افتح واقفل الاتصال لكل طلب** — لا تجميع/إعادة استخدام عبر reruns.
- **Arabic في SQL يكسر PostgreSQL:** استعلم بأسماء إنجليزية ثم أعد تسمية أعمدة
  DataFrame للعربي في Python قبل العرض.
- **`dashboard.py` ملف واحد ٢٨٠٠+ سطر**، كل الصفحات سلسلة `if/elif` على
  `page = st.sidebar.radio(...)`. **حذف/إضافة صفحة:** عدّل قائمة الراديو (قد تحوي
  عدة عناصر بنفس السطر — احذر حذف الأبرياء!) + بلوك `elif` المقابل. تحقّق بـ
  `python -m py_compile dashboard.py` بعدها.
- **قوالب blog.ts:** لا ``` (backticks ثلاثية) داخل template literals — تكسر SWC
  وتعطّل كل المقالات بعدها. راجع `feedback_no_backticks_in_template_literals.md`.
- **الدومين الصحيح `dealpulseksa.com`** (حيّ)؛ `dealpulesksa.com` توأم ميّت. لا
  تحكم على هجاء من لقطة مضغوطة — احسمه بـ curl/DNS.

---

## ٧) البيئة (ويندوز — مهم للأداء)

- **Python venv** في `venv/` (Python 3.13). **شغّل بـ `venv/Scripts/python` و
  `venv/Scripts/streamlit.exe`** لا الـ Python العام — وإلا الحزم مفقودة.
- الشِل الأساسي **PowerShell**؛ أداة Bash متاحة (POSIX). لكلٍّ صياغته.
- **الرام ٨.٥GB فقط ومزدحمة** (فايرفوكس/VS Code/Discord). أي شيء يحمّل نموذج ML
  ثقيل يرحّل للقرص ويزحف أو يفشل بـ MemoryError. اطلب إغلاق فايرفوكس عند الحاجة،
  ولا تشغّل `grep -r` ثقيل على كل المجلد بالتوازي مع مهمة ذاكرة (يخنقها).
- **تشغيل الداشبورد:** `venv/Scripts/streamlit.exe run dashboard.py`. الدخول
  `admin` / `123456` (bcrypt hash في `.streamlit/secrets.toml` — غير متتبّع في
  git، محلي فقط). streamlit لا يعيد تحميل الوحدات المستوردة تلقائياً — أعد تشغيله
  بعد تعديل ملف مستورَد.
- **قاعدة البيانات الحيّة:** الإنتاج عبر `DATABASE_URL` في `.env` (Railway، ٤٢
  متجر). localhost = وهمي/اختبار. راجع `db_local_vs_railway.md` +
  `single_source_of_truth.md`.

---

## ٨) عند إنهاء أي مهمة

1. تحقّق end-to-end (شغّل، أنتج مخرَجاً، أثبته).
2. حدّث الذاكرة لو فيه قرار/درس غير بديهي.
3. `git add` + commit برسالة تشرح **السبب الجذري** لا فقط ماذا، وذيّلها بـ
   `Co-Authored-By: Claude ...`.
4. اسحب لو الريموت متقدّم (حُلّ التعارض لصالح **المُتحقَّق منه عملياً**)، ثم ادفع.
5. اسأل نفسك: لو انهار هذا غداً، هل تركت أثراً كافياً ليفهمه Claude التالي؟

---

**الخلاصة:** لا تكن سريعاً غبياً. كن بطيئاً في التشخيص، حاسماً في التنفيذ، صادقاً
في التقييم، ومهووساً بالتحقق. اقرأ الذاكرة، افهم الجذر، أثبت المخرَج، وثّق للتالي.
